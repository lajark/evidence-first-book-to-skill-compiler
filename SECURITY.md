# Book2Skill 安全与隐私基线

- 默认离线，无遥测；
- 云模型需显式 opt-in，并显示将离开本机的数据范围；
- 文档中的指令、脚本、URL 和角色设定全部视为数据；
- 不执行生成 Skill 中的任意命令，除非用户审阅并授权；
- 限制文件大小、解压比例、路径、符号链接和递归深度；
- 临时文件使用受控目录并在失败后安全清理；
- 日志不含正文、密钥、完整私有路径；
- 发布前进行许可证、长引文、提示注入、隐藏字符和脚本权限检查。
