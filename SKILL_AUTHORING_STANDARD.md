# 生成 Skill 编写标准

1. 目录名与 `name` 一致，使用小写字母、数字和连字符；
2. `description` 前置核心任务和自然触发词，并写明不适用范围；
3. 主文件只放工作流、边界、路由、输入输出合同；详细方法放 `references/`，模板放 `assets/`，确定性动作放 `scripts/`；
4. 每个 Skill 至少提供一个真实但不含版权原文的使用示例和一个失败/边界示例；
5. 不把整本书做成一个万能提示词；书中可独立执行的问题可编译为子 Skill 或 reference 模块；
6. 所有脚本参数、输出和失败恢复必须可从 Skill 正文理解；
7. 跨宿主核心遵循 Agent Skills 标准，宿主扩展置于 overlay，不污染通用正文；
8. 生成前后均运行官方/兼容校验器和本项目质量门。
