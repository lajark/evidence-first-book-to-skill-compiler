# Project Instructions

> 本文件是项目级规则的单一事实来源，保存工具无关、需要团队共享的项目事实和约束。删除占位内容后提交到 Git。

## Project Overview
- 项目目标：<一句话说明项目解决的问题>
- 核心技术栈：<语言、框架、运行时、数据库>
- 主要入口：<应用、服务、CLI 或包的入口>
- 关键数据流：<必要时简述>

## Commands
- 安装依赖：`<command>`
- 本地启动：`<command>`
- 单元测试：`<command>`
- 指定测试：`<command>`
- Lint：`<command>`
- 类型检查：`<command 或不适用>`
- 构建：`<command>`
- 端到端/集成测试：`<command 或不适用>`

> 只保留仓库中真实存在且可运行的命令，不让 Agent 猜测包管理器或脚本名称。

## Architecture and Scope
- 新代码应放置在：<目录与职责>
- 层级依赖方向：<例如 controller → service → repository>
- 禁止跨层依赖：<明确红线>
- 必须复用的组件、服务或工具：<列表>
- 生成文件、供应商目录或兼容层：<不得手工修改的目录>
- 数据库迁移和 API 兼容要求：<约束>

## Coding Conventions
- 格式化与命名：<引用仓库配置或写明规则>
- 错误处理：<错误类型、返回格式、用户文案语言>
- 日志与监控：<日志库、敏感字段、级别>
- 配置与环境变量：<加载方式；不要填写真实密钥>
- 国际化：<语言及 i18n 约定>
- 文档与注释：<按语言/框架说明，不做跨语言一刀切>

## Testing and Definition of Done
- 按变更风险选择验证：先运行受影响范围的检查，再按需要扩大到完整 CI。
- 新增或改变关键行为时，覆盖正常路径、重要边界和主要失败路径。
- UI 变更：<截图、视觉回归或人工验收要求>
- 数据库/API 变更：<迁移、回滚、兼容性验证要求>
- 未实际执行的检查必须明确标注，不得写成“已通过”。

## Repository Etiquette
- 不修改任务范围外的代码，不顺手修复无关问题。
- 不提交密钥、构建产物、本机配置或临时文件。
- 未经明确要求，不提交、推送、发布或部署。
- 重大依赖升级、迁移和破坏性变更先说明影响、备份和回滚方案。

## Project-Specific Prohibitions
- 未经确认不得修改：<例如 migrations/、infra/、.github/workflows/>
- 其他禁区：<按项目填写>
